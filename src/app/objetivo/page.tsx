import { ObjectiveForm } from "@/components";

export default function Objective() {
  return (
    <main className="min-h-screen bg-gray-100 flex items-center justify-center p-4">
      <ObjectiveForm />
    </main>
  );
}
